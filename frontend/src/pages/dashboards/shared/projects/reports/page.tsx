import { PlaceholderPage } from '@/components/placeholder-page';
import { FileText } from 'lucide-react';

export default function ProjectReportsPage() {
    return (
        <PlaceholderPage
            title="Project Reports"
            description="Generate and view project analytics"
            icon={FileText}
            cardTitle="Project Reports"
            cardDescription="Access detailed project insights and reports"
        />
    );
}
